% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_transformations.R
\name{to_wide}
\alias{to_wide}
\title{Transform a dataset into wide format}
\usage{
to_wide(ds, x_axis = NULL)
}
\arguments{
\item{ds}{A dataset object.}

\item{x_axis}{Optional column name to use as value axis. If \code{NULL}, the stored x-axis is used.}
}
\value{
A dataset in wide format.
}
\description{
Converts the current dataset (long or decomposed) to wide format.
Use \code{x_axis} to specify the column that should become value columns.
}
\details{
This is a thin wrapper around \code{\link[=dataset_transfrom]{dataset_transfrom()}} with \code{to = "wide"}.
}
\examples{
\dontrun{
library(rDataSet)
ds <- dataset_build(tibble::tibble(id = 1:2, value = c("a", "b")), ids = "id")
long <- to_long(ds)
wide <- to_wide(long)
}
}
\seealso{
\code{\link[=to_long]{to_long()}}, \code{\link[=to_decomposed]{to_decomposed()}}, \code{\link[=dataset_transfrom]{dataset_transfrom()}}
}
