% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_composition.R
\name{efficient_paths}
\alias{efficient_paths}
\title{Efficient decomposition paths}
\usage{
efficient_paths(dataset)
}
\arguments{
\item{dataset}{A dataset object in wide format.}
}
\value{
A tibble with columns \code{ids} (list of character vectors) and
\code{paths} (list of character vectors), sorted by increasing
distinctness and ID count.
}
\description{
Generates all possible combinations of ID columns and orders them
by the number of distinct values (fewer first) and the number of
ID columns. This strategy can produce a more compact decomposition
but runs in exponential time with respect to the number of ID columns.
Use only when the number of ID columns is small (typically ≤ 10).
}
\examples{
\dontrun{
library(rDataSet)
ds <- dataset_build(
  tibble::tibble(id = 1:2, group = c("x","y"), value = 1:2),
  ids = c("id","group")
)
efficient_paths(ds)
}
}
\seealso{
\code{\link[=hirarchical_paths]{hirarchical_paths()}}, \code{\link[=selected_paths_builder]{selected_paths_builder()}}, \code{\link[=dataset_decompose]{dataset_decompose()}}
}
