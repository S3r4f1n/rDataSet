% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_composition.R
\name{selected_paths_builder}
\alias{selected_paths_builder}
\title{Build a custom decomposition strategy}
\usage{
selected_paths_builder(ids)
}
\arguments{
\item{ids}{Character vector of ID column names to prioritise.}
}
\value{
A function that takes a dataset and returns a tibble
with columns \code{ids} and \code{paths}, suitable for use as the
\code{strategy} argument of \code{\link[=dataset_decompose]{dataset_decompose()}}.
}
\description{
Returns a function that, when applied to a dataset, creates a
decomposition plan where the specified IDs are used first,
followed by the remaining IDs, and finally all IDs together.
This allows you to prioritise certain ID columns during
decomposition.
}
\examples{
\dontrun{
library(rDataSet)
ds <- dataset_build(
  tibble::tibble(id = 1:2, group = c("x","y"), value = 1:2),
  ids = c("id","group")
)
strat <- selected_paths_builder("group")
strat(ds)
}
}
\seealso{
\code{\link[=hirarchical_paths]{hirarchical_paths()}}, \code{\link[=efficient_paths]{efficient_paths()}}, \code{\link[=dataset_decompose]{dataset_decompose()}}
}
