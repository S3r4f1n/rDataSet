% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_transformations.R
\name{to_decomposed}
\alias{to_decomposed}
\title{Decompose a dataset into a list of data frames}
\usage{
to_decomposed(ds, strategy = hirarchical_paths, x_axis = NULL)
}
\arguments{
\item{ds}{A dataset object.}

\item{strategy}{A function that returns a tibble of hierarchical paths
(default: \code{\link[=hirarchical_paths]{hirarchical_paths()}}).}

\item{x_axis}{Optional column name for the value axis; if \code{NULL}, the
stored x-axis is used.}
}
\value{
A list of data frames (each inherits class \code{"dataset"})
in decomposed format.
}
\description{
Splits a wide or long dataset into a list of child data frames along
hierarchical ID paths, using the supplied decomposition strategy.
}
\details{
This is a thin wrapper around \code{\link[=dataset_transfrom]{dataset_transfrom()}} with \code{to = "decomposed"}.
}
\examples{
\dontrun{
library(rDataSet)
ds <- dataset_build(
  tibble::tibble(id = 1:2, group = c("x","y"), value = 1:2),
  ids = c("id","group")
)
decomp <- to_decomposed(ds)
}
}
\seealso{
\code{\link[=hirarchical_paths]{hirarchical_paths()}}, \code{\link[=dataset_compose]{dataset_compose()}}, \code{\link[=dataset_transfrom]{dataset_transfrom()}}
}
