% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_transformations.R
\name{dataset_transfrom}
\alias{dataset_transfrom}
\title{Convert a dataset to a given state}
\usage{
dataset_transfrom(
  ds,
  to,
  x_axis = NULL,
  decompose_strategy = hirarchical_paths
)
}
\arguments{
\item{ds}{A dataset object.}

\item{to}{Target state: one of \code{"wide"}, \code{"long"}, or \code{"decomposed"}.}

\item{x_axis}{Optional column name for the value axis (only used when
converting to wide or decomposed).}

\item{decompose_strategy}{Strategy for decomposition (only used when
converting to decomposed format).}
}
\value{
A dataset object in the requested state.
}
\description{
Low-level function that dispatches transformation logic based on current
and target state. Usually you should prefer the user-friendly wrappers
\code{\link[=to_wide]{to_wide()}}, \code{\link[=to_long]{to_long()}}, and \code{\link[=to_decomposed]{to_decomposed()}}.
}
\details{
The function handles all six possible transitions between wide, long, and
decomposed formats. It automatically composes or decomposes datasets as
required, and can optionally set a new x-axis column when converting to
wide or decomposed format.
}
\examples{
\dontrun{
library(rDataSet)
ds <- dataset_build(
  tibble::tibble(id = 1:2, val1 = "a", val2 = "b"),
  ids = "id"
)
long <- dataset_transfrom(ds, "long")
wide <- dataset_transfrom(long, "wide")
}
}
\seealso{
\code{\link[=to_wide]{to_wide()}}, \code{\link[=to_long]{to_long()}}, \code{\link[=to_decomposed]{to_decomposed()}}
}
